# Core server configuration
NODE_ENV=production
PORT=4000
LOG_LEVEL=info
# Optional: Path to log file (relative to project root or absolute path)
# If set, JSON logs will be written to this file in addition to console output
# LOG_FILE=server.log

# Lovdata public API (leave as-is unless proxying through another host)
LOVDATA_BASE_URL=https://api.lovdata.no
LOVDATA_TIMEOUT_MS=30000

# Public API base URL exposed to frontend/links
PUBLIC_API_BASE_URL=https://lovdata-assistant.example.com

# Supabase configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
SUPABASE_ANON_KEY=replace-with-anon-key

# OpenAI configuration (required for agent responses)
OPENAI_API_KEY=replace-me
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=1

# Serper configuration (optional fallback search)
SERPER_API_KEY=
SERPER_BASE_URL=https://google.serper.dev/search
SERPER_SITE_FILTER=lovdata.no

# Observability / tracing (optional)
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=
LANGSMITH_ENDPOINT=

